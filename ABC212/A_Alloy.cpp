#include <bits/stdc++.h>
#define int int64_t
using namespace std;

int32_t main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int a, b;   cin >> a >> b;
    if(a == 0)
        return cout << "Silver", 0;
    if(b == 0)
        return cout << "Gold", 0;
    cout << "Alloy";
}