#include <bits/stdc++.h>
#define int int64_t
using namespace std;

const int N = 5e3+5;
int dp[N][N], a[N], b[N];

int32_t main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    
    int n;  cin >> n;
    for(int i = 1; i <= n; i++) cin >> a[i];
    for(int i = 1; i <= n; i++) cin >> b[i];

    dp[]
    for(int i = 1; i <= 5000; i++)
    for(int j = 1; j <= n; j++)
    {
        dp[i][j] = 
    }

    for(int &x: a)  cin >> x;
    for(int &x: b)  cin >> x;
    for(int i = 0; i < n; i++) {

    }
}