#include <bits/stdc++.h>
#define int int64_t
using namespace std;

int32_t main()
{
  ios_base::sync_with_stdio(0);
  cin.tie(0);
  float a, b;
  cin >> a >> b;
  cout << fixed << setprecision(3) << b / a << endl;
}