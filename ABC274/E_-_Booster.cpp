#include <bits/stdc++.h>
#define int int64_t
using namespace std;

int32_t main()
{
  ios_base::sync_with_stdio(0);
  cin.tie(0);
  int n, m;
  int tx[n], ty[n];
  int cx[m], cy[m];
  for(int i = 0; i < n; i++) {
    cin >> tx[i] >> ty[i];
  }
  for(int i = 0; i < m; i++) {
    cin >> cx[i] >> cy[i];
  }
    
}