#include <bits/stdc++.h>
#define int int64_t
using namespace std;

int32_t main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int a, b;   cin >> a >> b;
    double c = (a + 2 * b) / 3.0;
    cout << fixed << setprecision(12) << c;
}