#include <bits/stdc++.h>
#define int int64_t
using namespace std;

int32_t main()
{
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	int t;	cin >> t;
	for(int _ = 1; _ <= t; _ ++) {
		int h, m, s, n;
		int a[3];
		for(int x: a)	cin >> x;
		sort(a, a + 3);
		do
		{
			int H = a[0], M = a[1], S = a[2];
			h = 			
		} while (next_permutation(a, a + 3));
		
		cout << "Case #" << _ << ": " << h << ' ' << m << ' ' << s << ' ' << n << '\n';
	}
}