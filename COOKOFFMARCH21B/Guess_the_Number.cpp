#include <bits/stdc++.h>
#define int long long
using namespace std;

int32_t main()
{
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	int t;	cin >> t;
	while(t--)
	{
		int q;
		for(int i = 1; i <= 2000; i++) {
			cout << i * i << endl;
			cin >> q;
			if(q)	break;
		}

	}
}