#include <bits/stdc++.h>
#define int int64_t
using namespace std;

void fun(string x, string y)
{
    int n = y.length();
    int m = x.length();
    int dp[n + 1][]
}

int32_t main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int t;	cin >> t;
    for(int i = 1; i <= t; i++)
    {
        cout << "Case " << i << ": ";
        string x, y;    cin >> x >> y;
        fun(x, y);
    }
}