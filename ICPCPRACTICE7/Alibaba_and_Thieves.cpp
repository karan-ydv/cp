#include <bits/stdc++.h>
#define int int64_t
using namespace std;

int32_t main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int t;	cin >> t;
    while(t--)
    {
        int n;	cin >> n;
        pair<int, int> a[n];
        for(auto &[x, y]: a)    cin >> x >> y;
        sort(a, a + n);
        int ans = 0;
        int T = -1;
        for(auto [x, y]: a)
        {

        }
    }
}